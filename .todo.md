

[] realar-project/todos
  [] javascript, hashchange, localstorage

[] Fix change deps bug
  [] TODO: remove rels from prev_deps (need to find diff between prev and new)

[] Rewrite slides to tutorial with new syntax and information about
  [] Try using SVG for documentation images! See size difference
  [] Add size for images
  [] Split all slice to more small images, try compact images from up to bottom

[] Remove "for of" constructions and migrate to es5 javascript for support usually compilers
  [] Check performance
[] Remove nested view construction, only one view start, and one view_end for one component
  [] nestes react component definition not supported

[] realar-project/ecommerce
  [] typescript, jest, node.js backand, ssr
  [] collection
  [] ssr

[] realar-project/social-network

[] Move unit method outside of unit definition (performance, see speed difference)
```javascript
const u = unit({
  a(arg) {
    return arg;
  }
});
// -->
function u_a_0(arg) { return arg; };
const u = unit(function() {
  const ctx = {};
  return [0,0,0,"a",u_a_0.bind(ctx)];
});
```

[] Throw exception for arrow function as unit method (javascript compatibility)

[] Fix destroy scopes


[] Add babel exception for using `arguments` inside unit methods
  [] Make performace tast for understanding speed difference between arrow function in transformed unit and binded function. If no difference to better using normal function with `arguments` support!
  [] Make special error doc page for answering about using `...args`

[] Add `scope` function for run logic for ssr or separate logic.
```javascript
const app = scope();
const user = app.runIn(() => shared(User)); // Think about
// or better
const user = app.shared(User);
const html = app.runIn(() => renderToString(<App />)); // Reseach about link/unlink in that case
const data = app.serialize(); // same app.runIn(() => serialize())
app.destroy();
```


[] Add inherit, clone
[] Direction to blazing fast.

[] Remove non javascript syntax

```javascript
const u = unit({
  m: 10,
  fn: () => this.m = 10; // This is non javascript syntax
  fn() {
    return this.m = 10; // Better!
  }
});
```

[] Stop unit reaction after unlinked

---

[] Inheritance

```javascript
const A = unit({
  constructor(arg) {}
});
const B = inherit(A, {
  constructor(arg) {
    this[parent].constructor(arg);
    // parent is symbol
    // I think is possible to make normal typings
  }
});
```

---

[] Remove magic [index] linsters, support only typescript compatible syntax

---
High Priorty bugs!

[] `arguments` keyword is not worked in unit methods
  [] Think about It, one of:
    1. wrap to `function() {}.bind(this)`;
    2. make babel preprocess of `arguments` keyword;
  [] Choose better performance

[] I need wrap only JSX return functions with arguments or useUnit, useService inside
[] automemo


---

[] Rewrite babel plugin to typescript
[] Rewrite babel plugin from templates to nodes manipulation

[] Debug, Tracing states. (devtools)


[] Add command line interface for quick make realar app
```bash
npx realar create todo-list
```
  Try https://github.com/vadimdemedes/ink
      https://github.com/vadimdemedes/pastel

[] Write contribution section to readme. Can contribute anybody and as your wish.

[] Site examples:
  https://atomicojs.github.io/doc

[] Add optional parameter for babel compilter: automemo. It will be all components with props or useUnit, useService wrapped to React.memo. And It will be performance fix for disable wrapping for components without parameters or useUnit, useService calls.

[] Add interactive sandbox for try Realar online same as effector (https://share.effector.dev/)


---


[] Remove magic [index] linsters (think about...)

```javascript
const a = action();
const u = unit({
  [a]() { },
  [a]: () => {},

  // I think Its better, but... I'm not sure))
  // Its can be typescript and javascript difference
  constructor() {
    on(a, aHandler);
    // Think about rename to linster
    // listen(a, aHandler); // so so
  },
  aHandler() { }
});
```

[] Think about pass changed values
```javascript
    ({ userId, flare }) => {
      const user = useOwn(User, userId);
      user.setFlare(flare); // And necessary to make optimization
      // decrease count of rerender as strong as possible

      // ...
    }
```

Brainstorm in next block
```javascript
    ({ userId, flare }) => {
      const user = useOwn(User, [userId], [flare]); // ??
      // parameters lifecycle method
      // or
      const user = useOwn(User, userId);
      useParameters(user.setFlare, flare);
      useChanges(user.setFlare, flare);
      user.setFlare(flare); // )))

      // That case:
      if (changed(flare)) {
        user.setFlare(flare);
        // Or but so so
        // May be it possible to move inside unit
      }

      useUpdate(user, flare);
      useChanged(user.updateFlare, [flare]);
      useChanges(user.updateFlare, [flare]);
      // May be, first think.
    }

    const User = unit({

    })
```



[] Better async pool decision
  [] Think about rename `signal` to shared. For prevent local and global name collision.

```javascript
async load() {
  const { cancelled, signal, cancel<Promise> } = await debounce(300); // Same as a hook. May be give minimal abstraction for user custom one.
  const { data } = await axios('/users', { signal });
  // // ...
  // if (!concelled()) // Rarely case. If we use signal, and Abort exception, all will be automatic
  return data;
},
async load() {
  const { signal } = await takeLast();
  // ...
  const { data } = await axios('/users', { signal });
  return data;
}
```

```javascript
async load() {
  await debounce(300, []: args); // Think about It (`[]: args` is the one of kind for uniq)
  await throttle(300, []: args);
  await serial([]: args);
  await parallel(1, []: args); // Read about stream pattern operations for better understanding

  await takeLast(); // Read about more redux saga operations!!


  /*
  take_last operation schema:
  promise ->     promise change you resolve to new load result
    load         load:again
  */

}
```

---



---

```javascript
constructor() {
  this.load.throttle(); // What about cancelling
  this.load.debounce(300); // Cancel on destruction

  // I want make not all operations:
  // 1. Each call will be cancel previous. And previous promise will be resolved with new value.
  // 2. Independent
},
async load() {
  //
  stoppable()
  abortable()
  cancellable()
  //

  const { cancelled, signal } = cancellable(); // Think...
  // ...

  await fetch("http://localhost:8000", { signal });

  if (cancelled()) { // Babel parsed, its possible to use inside async promise then function
    break;
  }
}
```

---

[] Function `loop` to constructor section for making infinity loop (Think about It)

```javascript
const u = unit({
  constructor() {
    loop(this.async_loop); // Todo: Hmmm... configure of async pool...
  },
  async async_loop() {
    await backend_reset_action;
    // ..
  }
})
```

---
High level bug:

```javascript
async load() {
  await delay(1);
  this.user = UserUnit(); // Here in constructor service resolving will be broken
  // I can resolve It throw using method
  // this.user = this.createUserUnit(); // And In that point to possible restore context
}
```

Think about It. I can type about It and give decision:
```javascript
addUser() { // And restore zone for each function context for fix async operations
  this.user = UserUnit();
},
async load() {
  await delay(1);
  this.addUser();
}
```

And I can write babel plugin for restore context after each await operation



---
[] Make boilerplate project
[] Make first production project

---
[] Thinks about unit function `unit.combine`

```javascript
let u1 = unit({
  a: 11,
  b() {}
});

let u2 = unit({
  a: 12,
  m() {}
});

let u_ = unit.combine(u1, u2); // { a: 12, b() {}, m() {} }
```

[] For accessing to parent method in override func
  your can use `super` keyword for call overrided function. (Optional)

```javascript
let u2 = unit({
  b() {
    super();
  }
});

let u_ = unit.combine(u1, u2);
```

---

[] Modern async operations

[] Fix both box bugs
  (One from old perf test, second from todo comment in core box code)

[] View performance test
    [] mobx-react

[] SSR
