import { core } from ".";

const {
  arr_create, arr_len, arr_push, arr_pop, arr_unshift, arr_shift, arr_free
} = core;

export * from ".";
export {
  arr_create, arr_len, arr_push, arr_pop, arr_unshift, arr_shift, arr_free
};
