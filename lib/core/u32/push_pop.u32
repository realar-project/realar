
func push

func pop

# TODO:
# - `push` should be change start byte pointer to new place (make new machine and run `init`)
# - `pop` should be change start byte pointer to previous value (and run `free`)
