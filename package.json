{
  "name": "realar",
  "version": "0.0.2-beta.4",
  "description": "Next generation frontends",
  "repository": {
    "url": "https://github.com/betula/realar"
  },
  "bugs": {
    "url": "https://github.com/betula/realar/issues"
  },
  "license": "MIT",
  "scripts": {
    "bootstrap": "npx lerna bootstrap --no-ci && npm run build",
    "clean": "npm run test:clean && npm run demo:clean && npm run build:clean && npm run perf:clean && lerna clean --yes",
    "publish": "npm run test && npm run build && lerna publish",
    "start": "npm run bootstrap && npm run demo",
    "reinstall": "npm run clean && npm run bootstrap",
    "test": "npm run test:clean && cross-env REALAR_DEV=true jest",
    "test:clean": "jest --clearCache",
    "demo": "npm run demo:clean && cd demo && cross-env NODE_ENV=development parcel index.html --port 1210",
    "demo:clean": "cd demo && rimraf dist .cache",
    "build:clean": "rimraf build",
    "build": "rollup -c",
    "build:watch": "cross-env REALAR_DEV=true npm run build && chokidar \"lib/**\" -c \"cross-env REALAR_DEV=true rollup -c\"",
    "lerna:clean": "lerna run clean",
    "perf:realar": "cd perf/realar && cross-env NODE_ENV=development parcel index.html --port 1200",
    "perf:mobx": "cd perf/mobx && cross-env NODE_ENV=development parcel index.html --port 1201",
    "perf": "concurrently --kill-others \"npm run perf:realar\" \"npm run perf:mobx\"",
    "perf:clean": "rimraf perf/*/dist perf/*/.cache"
  },
  "author": "Slava Birch <mail@betula.co> (http://betula.co)",
  "keywords": [
    "state",
    "model",
    "reactive",
    "state management",
    "data flow",
    "react hooks",
    "micro frontends",
    "next"
  ],
  "main": "build/index.js",
  "module": "build/index.esm.js",
  "types": "realar.d.ts",
  "files": [
    "babel",
    "jest",
    "build",
    "realar.d.ts"
  ],
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "devDependencies": {
    "@babel/core": "7.11.1",
    "@babel/plugin-proposal-decorators": "7.10.5",
    "@babel/preset-env": "7.11.0",
    "@babel/preset-react": "7.10.4",
    "@rollup/plugin-node-resolve": "8.4.0",
    "babel-jest": "26.2.2",
    "chokidar-cli": "2.1.0",
    "concurrently": "5.3.0",
    "cross-env": "7.0.2",
    "deasync": "0.1.20",
    "enzyme": "3.11.0",
    "enzyme-adapter-react-16": "1.15.3",
    "jest": "26.2.2",
    "lerna": "3.22.1",
    "mobx": "5.15.5",
    "mobx-react": "6.2.5",
    "parcel": "1.12.4",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "rimraf": "3.0.2",
    "rollup": "2.23.1",
    "wabt": "1.0.19"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ],
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "alias": {
    "realar": "./build"
  }
}
